{
  "name": "rnb-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r --filter ./packages... build",
    "clean": "pnpm -r --filter ./packages... clean"
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
